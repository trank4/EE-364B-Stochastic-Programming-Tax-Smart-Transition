[project]
name = "stochastic-optimizer"
version = "0.1.0"
description = "Stochastic portfolio transition optimizer using Gurobi"
authors = [
    {name = "trank4"}
]
requires-python = "^3.11"
dependencies = [
    "gurobipy>=11.0.0",
]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"